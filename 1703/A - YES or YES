#include <bits/stdc++.h>
using namespace std;
 
int main() {
    int n;
    cin >> n;
    while (n--) {
        string s;
        cin >> s;
 
    
        for (auto &c : s) c = tolower(c);
 
        if (s == "yes") cout << "YES
";
        else cout << "NO
";
    }
    return 0;
}